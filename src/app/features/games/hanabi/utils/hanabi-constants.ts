export class HanabiConstants {
  public static readonly DESCRIPTION_CRITICAL_CARD: string = `Critical cards must be played to reach the maximum score but are only present in a single copy.`;
  public static readonly DESCRIPTION_TRASH_CARD: string = `Trash cards will never be played again and can be discarded safely.`;

  public static readonly DESCRIPTION_REQUIRED_EFFICIENCY: string = `Required efficiency is the ratio between 'remaining cards to clue' and 'remaining clues'.
                                                                    The lower it gets the better it is.`;
  public static readonly DESCRIPTION_PACE: string = `The number of cards you can still discard in order to reach max score.`;
}
